generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model roles_table {
  role_id   Int           @id @default(autoincrement())
  role_name String        @unique @db.VarChar(50)
  users     users_table[]
}

model users_table {
  user_id       Int         @id @default(autoincrement())
  full_name     String      @db.VarChar(100)
  email         String      @unique(map: "idx_users_table_email_unique") @db.VarChar(100)
  password_hash String      @db.VarChar(255)
  address       String?
  role_id       Int
  created_at    DateTime?   @default(now()) @db.Timestamp(6)
  role          roles_table @relation(fields: [role_id], references: [role_id])

  @@index([email], map: "idx_users_table_email")
}

model categories_table {
  category_id   Int    @id @default(autoincrement())
  category_name String @unique @db.VarChar(100)
}

model products_table {
  product_id     Int       @id @default(autoincrement())
  category_id    Int
  name           String    @db.VarChar(150)
  description    String?
  price          Decimal   @db.Decimal(10, 2)
  stock_quantity Int
  created_at     DateTime? @default(now()) @db.Timestamp(6)
  image_URL      String?

  @@index([category_id], map: "idx_products_table_category")
}

model orders_table {
  order_id       Int      @id @default(autoincrement())
  user_id        Int
  order_date     DateTime @default(now()) @db.Timestamp(6)
  status         String?  @default("Pending") @db.VarChar(50)
  total_amount   Decimal? @default(0) @db.Decimal(10, 2)
  payment_status String?  @default("pending") @db.VarChar(50)

  @@index([user_id], map: "idx_orders_table_user")
}

model order_items_table {
  order_item_id Int     @id @default(autoincrement())
  order_id      Int
  product_id    Int
  quantity      Int
  price         Decimal @db.Decimal(10, 2)
  subtotal      Decimal @db.Decimal(10, 2)

  @@index([product_id], map: "idx_orderitems_table_product")
}

model payments_table {
  payment_id     Int       @id @default(autoincrement())
  order_id       Int
  payment_date   DateTime? @default(now()) @db.Timestamp(6)
  amount         Decimal   @db.Decimal(10, 2)
  payment_method String    @db.VarChar(50)
  payment_status String?   @default("Successful") @db.VarChar(50)
  user_id        Int?

  @@index([order_id], map: "idx_payments_table_order")
}

model employee {
  id     Int?
  name   String? @db.VarChar(50)
  salary Int?

  @@ignore
}

model person {
  id   Int?
  name String? @db.VarChar(50)

  @@ignore
}

model students {
  student_id Int?
  name       String? @db.VarChar(50)
  age        Int?

  @@ignore
}
